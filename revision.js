function setup () {
    createCanvas (600,600)
}
function preload(){
    //assets
}
function draw() {
//animations 
//physics
    if(mouse.presses ()){
        let cube = new Sprite(mouseX, mouseY, 50,50)
    }
}